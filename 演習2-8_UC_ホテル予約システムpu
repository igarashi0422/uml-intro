@startuml
left to right direction
actor 利用者
actor ホテル従業員
actor ホテル管理者

rectangle "ホテル予約システム" {
  利用者 --> (ホテルの空室状況を閲覧する)
  利用者 --> (ホテルの部屋を予約する)
  (ホテルの部屋を予約する) --> (システムにログインする) : <<include>>
  (システムにログインする) <-- (会員登録を行う) : <<extends>>
  ホテル従業員 --> (空室状況を入力する)
  ホテル管理者 ---> (予約の集計を行う)
  ホテル従業員 --> (システムにログインする)
  ホテル管理者 --> (システムにログインする)
  利用者 --> (会員登録を行う)
  (会員登録を行う) <-- (予約を行わずに会員登録する) : <<extends>>
  (ホテルの部屋を予約する) --> (宿泊費がキャンペーン価格になる) : <<includes>>
}
@enduml
